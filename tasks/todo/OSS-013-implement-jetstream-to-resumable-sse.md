# OSS-013 — Implement JetStream to resumable SSE

## HTTP streaming

Define consumer ownership, start positions, `Last-Event-ID`, acknowledgement policy, redelivery, disconnect behavior, retention constraints, and safe cleanup.
