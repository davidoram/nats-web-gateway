# OSS-008 — Add ADR-32 discovery and compatibility tests

## HTTP request/reply

Verify service endpoint conventions, status/discovery interoperability, queue
behavior, metadata, and error semantics against a real NATS service.
