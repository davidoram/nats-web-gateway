# OSS-014 — Add streaming load and resilience tests

## HTTP streaming

Exercise slow clients, reconnects, server restarts, Caddy reloads, buffer exhaustion, cancellation races, and resource-leak detection.
